# Life Admin Agent — Backlog

> Единственный канонический список задач проекта.
> Порядок разделов = порядок реализации.

---

## Архитектурные решения (зафиксировано)

| Вопрос | Решение |
|--------|---------|
| Telegram | aiogram, webhook (не polling) |
| Scheduler | APScheduler внутри FastAPI Web Service |
| Render tier | Starter (не засыпает) |
| Email inbound | Заглушка в MVP, Mailgun в backlog |
| OpenAI Vision | Не оптимизируем пока |
| owner_id | Поле есть (задел на v2), логика одного пользователя |
| Промпты | Всегда в `prompts/*.txt`, никогда в Python |
| Порядок сборки | Вертикальные срезы |
| Тесты | После среза end-to-end, до следующего среза |
| Паттерны тестов | Из `calendar_bot/tests/` (mock, conftest, pytest-asyncio) |

---

## 🔴 High Priority

### [ARCH-006] Конституция агента: `prompts/soul.txt`

**Статус:** 🆕 To Do
**Приоритет:** High
**Компонент:** Prompting / Parser

**Описание проблемы**
Агент отвечает технически корректно, но не имеет зафиксированной «конституции» поведения: нет единого источника правил тона, языка, уровней автономии и шаблона принятия решений при неоднозначности.

**Решение**
- Добавить файл `prompts/soul.txt`, который читается на каждом запросе как часть system prompt.
- Зафиксировать в `soul.txt`:
  - роль: «личный операционный диспетчер пользователя»;
  - тон: дружелюбный коллега без лишних реверансов;
  - язык: всегда русский;
  - даты: формат `DD.MM.YYYY`;
  - длину: коротко и по делу, «одно действие — одно сообщение»;
  - уровни автономии: «выполнять молча / уведомить после / спросить сначала»;
  - поведение при нехватке данных: одно уточнение;
  - поведение при неоднозначности: выбрать наиболее вероятный вариант и сообщить.
- Подключить чтение `soul.txt` в формирование system prompt в `modules/parser.py` и `modules/reference.py`.

---

## 🟡 Medium Priority

### [UX-006] Отправка нескольких файлов одним медиа-альбомом

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** `bot/client.py`, `bot/handlers.py`

**Описание проблемы**
Когда у записи справочника несколько файлов (r2_keys), бот отправляет их последовательно отдельными сообщениями. Если все файлы — фотографии, их можно отправить одним альбомом (`sendMediaGroup`) — это удобнее и чище выглядит в чате.

**Ожидаемое поведение**
- Если все файлы в `r2_keys` — изображения (jpg/png) и их не более 10 — отправить как медиа-альбом одним сообщением с данными как caption к первому фото
- Если в списке есть PDF или файлов более 10 — отправлять по очереди (текущее поведение)

**Технические детали**
- Добавить `send_media_group(chat_id, items: list[dict]) -> dict` в `bot/client.py`
- `items` = список `InputMediaPhoto` / `InputMediaDocument` (Telegram Bot API)
- В `bot/handlers.py` в блоке отправки файлов по запросу: определить тип каждого файла по расширению, выбрать стратегию

**Acceptance Criteria**
- [ ] При `пришли права` с 2 фото — приходит один альбом, а не 2 отдельных сообщения
- [ ] При `пришли права` с PDF + фото — приходят последовательно (fallback)
- [ ] `make check` проходит

---

### [UX-007] Приём нескольких фото одним сообщением от пользователя

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** `bot/handlers.py`

**Описание проблемы**
Telegram позволяет пользователю отправить несколько фото одним альбомом (media group). Сейчас каждое фото из альбома обрабатывается независимо — создаются отдельные записи справочника. Пользователь может захотеть отправить три страницы паспорта или две стороны прав как один альбом с одним caption.

**Ожидаемое поведение**
Если пользователь отправляет альбом из нескольких фото с caption `справочник: водительские права` — все фото должны сохраняться в одну запись справочника (в `r2_keys`), а не создавать N отдельных записей.

**Технические детали**
- Telegram присылает несколько апдейтов с одинаковым `media_group_id` почти одновременно
- Нужен механизм буферизации: накапливать фото с одинаковым `media_group_id` в течение ~1с, затем обрабатывать как группу
- Вариант 1: in-memory dict `{media_group_id: [photos]}` + asyncio таймер на 1с
- Вариант 2: `bot_state` таблица (будет после UX-001)
- Caption берётся от первого фото в группе
- Зависит от FEATURE-015 (`r2_keys` должен существовать) и UX-001 (state storage)

**Acceptance Criteria**
- [ ] Альбом из 2 фото с caption `справочник: права` → одна запись с `r2_keys` из 2 ключей
- [ ] Каждое фото без альбома обрабатывается как раньше
- [ ] `make check` проходит

---

### [UX-008] FSM — уточнить матчинг поездки при генерации чеклиста

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** `bot/handlers.py`, `modules/suggestions.py`

**Описание проблемы**
Пользователь пишет «поездка во Вьетнам», но entity называется «Рейс SU 0294 Москва — Нячанг». Агент не находит совпадение по ilike и не привязывает чеклист к entity.

**Ожидаемое поведение**
Если entity не найдена по точному совпадению — агент переспрашивает: «Вьетнам из запроса — это Нячанг 20-25 марта?» (предлагает ближайший trip entity). После подтверждения — генерирует и привязывает.

**Acceptance Criteria**
- [ ] Нечёткий матчинг: если нет точного ilike — предложить ближайший trip entity
- [ ] FSM: кнопки «Да, эта поездка» / «Нет, без привязки»
- [ ] `make check` проходит

**Зависит от:** UX-001 (state machine)

---

### [UX-009] FSM — уточнить тип поездки и состав при генерации чеклиста

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** `bot/handlers.py`, `modules/parser.py`

**Описание проблемы**
Из запроса «собери чеклист для поездки в Дубай» непонятно — отдых или командировка, едет один или с семьёй. Оба параметра влияют на содержание чеклиста.

**Ожидаемое поведение**
Если тип и/или состав не ясны из текста — агент задаёт один уточняющий вопрос: «Это отдых или командировка? Едешь один?». Ответ сохраняется в state, чеклист генерируется после ответа.

**Acceptance Criteria**
- [ ] Если trip_type=null или travelers=null в parse_trip_checklist_request → задать вопрос
- [ ] После ответа — генерация с корректными параметрами
- [ ] `make check` проходит

**Зависит от:** UX-001 (state machine)

---

### [UX-010] Кнопка «✈️ Создать поездку» — полный callback handler

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** `bot/handlers.py`, `modules/lifecycle.py`

**Описание проблемы**
При генерации чеклиста без привязанной поездки бот показывает кнопку «✈️ Создать поездку». Сейчас при нажатии отвечает текстовой инструкцией. Нужен полный handler который создаёт entity.

**Ожидаемое поведение**
При нажатии `create_trip_{destination}` — создаётся entity типа `trip` с именем из параметра, бот отвечает «✈️ Создал поездку: {destination} · /entity {id}», предлагает прикрепить чеклист.

**Acceptance Criteria**
- [ ] Нажатие кнопки создаёт entity type=trip
- [ ] Бот отвечает подтверждением со ссылкой на entity
- [ ] `make check` проходит

---

### [UX-005] Dialog state для generate_text — ответ на уточняющий вопрос

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** `bot/handlers.py`, `modules/reference.py`

**Описание проблемы**
Когда `generate_text` возвращает уточняющий вопрос ("У тебя несколько машин, уточни"), пользователь отвечает коротко: "1" или "1 и 1". Бот не понимает контекст и создаёт случайный entity ("Мелкая задача") вместо генерации текста.

**Ожидаемое поведение**
После уточняющего вопроса бот запоминает pending-контекст (`_pending_generate_context`). Следующее сообщение пользователя интерпретируется как уточнение и передаётся в `generate_text` вместе с исходным запросом.

**Технические детали**
- Аналог `_pending_edit_entity_id` — глобальная переменная `_pending_generate_request: str | None`
- Определить: был ли ответ OpenAI уточняющим вопросом (содержит нумерованный список + вопрос)
- Если да — сохранить исходный запрос в `_pending_generate_request`
- Следующее сообщение: объединить `_pending_generate_request + " " + text` и снова вызвать `generate_text`
- Сбросить после генерации или через timeout

**Критерии приёмки**
- [ ] "напиши пропуск на меня и Toyota" → уточняющий вопрос → "1 и 1" → готовый пропуск
- [ ] Если пользователь написал полный запрос — state не активируется
- [ ] State сбрасывается после успешной генерации
- [ ] `make check` проходит

---

### [FEATURE-013] Импорт всех событий и дней рождения из Google Calendar

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** `modules/google_calendar.py`, `modules/ingestion.py`, `models.py`

**Описание проблемы**
Текущая интеграция с Google Calendar создаёт события и проверяет конфликты, но не подтягивает уже существующие планы пользователя. Дни рождения из Google Calendar не обрабатываются.

**Ожидаемое поведение**
- По API Google Calendar агент забирает все события из выбранных календарей за заданный горизонт (12 месяцев назад + 24 месяца вперёд).
- Отдельно подтягиваются дни рождения.
- Импортированные события отображаются как сущности без дублей.

**Технические детали**
- Добавить в `modules/google_calendar.py` методы:
  - `list_all_events(calendar_ids, time_min, time_max)` — пагинированный сбор событий (`nextPageToken`)
  - `list_birthdays(time_min, time_max)` — из `#contacts@group.v.calendar.google.com`
- Синхронизация в `modules/ingestion.py` или отдельный sync-модуль:
  - нормализация событий Google → внутренний формат;
  - upsert по `google_event_id` для защиты от дублей;
  - логирование в `event_log` (`action=calendar_sync`).
- Расширить модель: поля `external_source`, `external_id`.
- Планировщик: ежедневный инкрементальный sync + ручной запуск «синхронизируй календарь».

**Критерии приёмки**
- [ ] Система импортирует события из Google Calendar за указанный диапазон (с пагинацией)
- [ ] Система импортирует дни рождения и создаёт соответствующие записи
- [ ] Повторный sync не создаёт дублей (upsert по `external_id`)
- [ ] Ошибки Google API фиксируются в `event_log`, не падают webhook/scheduler
- [ ] Команда «синхронизируй календарь» запускает ручную синхронизацию
- [ ] Добавлены тесты на пагинацию, дедупликацию и импорт дней рождения

---

### [UX-004] Редактирование записи справочника через бот

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** `bot/handlers.py`, `modules/reference.py`

**Описание проблемы**
После сохранения записи в справочнике нет способа исправить ошибку через бот — только SQL-запрос в Render Dashboard.

**Ожидаемое поведение**
Пользователь отправляет боту инструкцию на естественном языке:
```
/ref edit 3 исправь фамилию на Добрынин
```
или нажимает кнопку ✏️ в карточке `/ref <id>`.

**Технические детали**
- Добавить кнопку `✏️ Изменить` в `make_ref_card_buttons`
- Callback `ref_edit_<id>` → устанавливает `_pending_ref_edit_id` (аналог `_pending_edit_entity_id`)
- Следующее текстовое сообщение → OpenAI патчит `data` JSON записи
- Альтернатива: команда `/ref edit <id> <инструкция>` без state machine

**Критерии приёмки**
- [ ] `/ref 3` показывает кнопку ✏️ Изменить
- [ ] После нажатия кнопки бот запрашивает инструкцию
- [ ] Инструкция "исправь фамилию на Добрынин Валентин Самсонович" корректно патчит `data.full_name`
- [ ] `/ref 3` после правки показывает обновлённые данные
- [ ] Тесты покрывают патчинг поля через OpenAI
- [ ] `make check` проходит

---

### [UX-001] State machine для прикрепления файлов

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** Bot / Storage

**Описание проблемы**
Два сценария с одним корневым ограничением — бот не помнит контекст между сообщениями:

1. Кнопка 📎 отвечает «Отправь файл с caption #ID». Пользователь не знает ID и не должен его знать. Если файл отправлен без caption — создаётся новый объект вместо прикрепления к существующему.
2. После сохранения первой страницы документа в справочник бот не предлагает добавить следующую страницу. Пользователь вынужден вручную слать `справочник #<id>`.

**Ожидаемое поведение**
State machine покрывает оба сценария:
- После нажатия 📎 на entity-карточке: бот запоминает `entity_id`, следующий файл прикрепляется автоматически
- После сохранения записи справочника: бот предлагает кнопку «📎 Добавить ещё страницу». Пользователь нажимает → файл добавляется к той же записи. Нажимает «✅ Готово» → запись закрывается

**Технические детали**
- Новая таблица `bot_state` — хранит `{chat_id, state, target_id, target_type, expires_at}`
  - `state` ∈ `awaiting_entity_file | awaiting_ref_page`
  - `target_type` ∈ `entity | reference`
- В `bot/handlers.py` перед обработкой любого файла — проверять активное состояние
- Состояние сбрасывается после успешного прикрепления или через 10 минут

**Acceptance Criteria**
- [ ] После нажатия 📎 бот запоминает `entity_id` в состоянии
- [ ] Следующий файл прикрепляется к нужному entity без caption
- [ ] После сохранения записи справочника бот показывает кнопку «📎 Добавить ещё страницу»
- [ ] Нажатие кнопки → следующий файл добавляется в `r2_keys` той же записи
- [ ] Кнопка «✅ Готово» закрывает состояние явно
- [ ] Состояние сбрасывается через 10 минут
- [ ] Caption `#ID` и `справочник #<id>` по-прежнему работают как fallback
- [ ] Тесты: state transition, timeout, прикрепление для обоих сценариев
- [ ] `make check` проходит

**Зависимости**
- FEATURE-015 должна быть выполнена (✅ Done)

---

### [FEATURE-012] Кастомные правила heartbeat через бота

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** Scheduler / Reminders / Bot

**Описание проблемы**
Сейчас heartbeat-логика жёстко зашита в `scheduler.py`. Пользователь не может добавлять собственные проактивные проверки без изменения кода.

**Решение**
- Добавить поддержку пользовательских правил расписания (weekly/monthly/cron)
- Создание правил через Telegram-бота на естественном языке:
  - «Напоминай мне каждую пятницу проверить бюджет» → recurring weekly reminder
  - «Каждое 1-е число месяца присылай список активных подписок» → monthly digest trigger
- Обновить scheduler, чтобы читал пользовательские правила из БД

**Критерии приёмки**
- [ ] Пользователь может создать минимум weekly и monthly кастомные правила через бота
- [ ] Кастомные правила сохраняются в БД и переживают рестарт сервиса
- [ ] Scheduler исполняет кастомные правила по расписанию
- [ ] Для правил есть базовое управление: просмотр списка и отключение
- [ ] Добавлены тесты на парсинг, сохранение и исполнение кастомных расписаний

---

### [ARCH-007] Skill-based маршрутизация действий вместо keyword detection

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** Parser / Prompting

**Описание проблемы**
`detect_intent` в `modules/parser.py` основан на жёстком списке ключевых слов. По мере роста функционала это приводит к конфликтам триггеров, ложным срабатываниям и усложняет сопровождение.

**Решение**
- Перейти к маршрутизации по смыслу через LLM: передавать в system prompt список доступных actions с описаниями, поручать модели выбор action по контексту запроса.
- Оставить keyword-логику как fallback на переходный период.

**Критерии приёмки**
- [ ] В prompt передаётся актуальный список доступных действий с описаниями
- [ ] Модель корректно выбирает action на типовых сценариях без keyword-триггеров
- [ ] Доля ложных срабатываний не хуже текущей реализации
- [ ] Добавлены тесты/фикстуры на маршрутизацию по смыслу

---

## 🟢 Low Priority

### [FEATURE-008] Email inbound через Mailgun

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** Bot / Ingestion

**Описание проблемы**
Пользователь пересылает письмо на Mailgun-адрес → агент парсит содержимое и создаёт entity.

**Ожидаемое поведение**
Пересланное письмо (бронь, билет, счёт) → entity в БД → подтверждение в Telegram.

**Технические детали**
- `bot/email_handler.py`: принимает Mailgun inbound POST
- Извлекает from, subject, body (text + html)
- Передаёт в `ingestion.process_text`
- Настройка Mailgun: входящие письма → POST на `/webhook/email`

**Почему Low Priority**
Требует настройки домена и Mailgun account. Не блокирует MVP-ценность.

**Критерии приемки**
- [ ] `/webhook/email` принимает Mailgun payload без 500
- [ ] Письмо создаёт entity в БД
- [ ] Подтверждение отправляется в Telegram
- [ ] Тесты: `test_email_handler.py`
- [ ] `make check` проходит

---

### [FEATURE-009] Web UI (v2)

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** Frontend

**Описание проблемы**
Визуальный кабинет для просмотра и управления объектами. Не в MVP.

**Технические детали**
- Реализация /api/* роутов (убрать 501)
- Frontend (React или Next.js — решить при старте)
- Auth через API_KEY или OAuth

**Критерии приемки**
- [ ] /api/* роуты возвращают реальные данные
- [ ] UI показывает список entities с фильтрами
- [ ] UI позволяет создавать и редактировать entities

---

### [UX-002] Страница объекта с прикреплёнными файлами (Web UI)

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** API / Frontend

**Описание проблемы**
В веб-кабинете нет возможности посмотреть файлы, прикреплённые к объекту. Особенно критично для составных объектов (поездка: билет + бронь + страховка).

**Ожидаемое поведение**
На странице карточки объекта — список прикреплённых файлов из таблицы `resources` с кнопкой скачать / открыть (signed URL из Cloudflare R2).

**Технические детали**
- Реализовать эндпоинт `GET /api/entities/{id}/resources`
- Signed URL генерировать через `modules/storage.get_presigned_url`
- Реализовывать в рамках FEATURE-009 (Web UI)

**Критерии приемки**
- [ ] `GET /api/entities/{id}/resources` возвращает список ресурсов
- [ ] Для `r2_key` — URL генерируется как presigned (1 час)
- [ ] `make check` проходит

---

### [OPS-003] Тихий heartbeat: явные логи `nothing to do`

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** Scheduler / Observability

**Описание проблемы**
Когда джобы отрабатывают без действий, в логах нет явного сигнала «всё в порядке, делать нечего». Это затрудняет отличение нормального пустого прогона от тихого сбоя.

**Решение**
- Добавить явные info-логи для пустых прогонов: `check_reminders_job: nothing to do`, `lifecycle_check_job: no entities to archive`, `monthly_review_job: nothing stale`
- Не слать пустые сообщения пользователю (уже соблюдается)

**Критерии приёмки**
- [ ] Для каждого heartbeat job есть явный `info`-лог при пустом прогоне
- [ ] Логи позволяют отличить «успешно, пусто» от ошибок исполнения
- [ ] Добавлены тесты на ветки `nothing to do`

---

### [ARCH-008] Уровни автономии для внешних действий (confirm-before-execute)

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** Action Model / Bot UX

**Описание проблемы**
В будущих интеграциях (оплата, бронирование, отправка email) нужен явный механизм подтверждения, чтобы агент не выполнял критичные действия автономно.

**Решение**
- Расширить модель действий полем `requires_confirmation: bool`
- Для внешних действий классов «оплатить / забронировать / отправить» всегда `requires_confirmation=true`
- Подтверждение через Telegram inline-кнопки

**Критерии приёмки**
- [ ] Action-модель поддерживает флаг `requires_confirmation`
- [ ] Внешние действия не исполняются без явного подтверждения пользователя
- [ ] В `event_log` фиксируются шаги: предложено / подтверждено / исполнено

---

### [UX-003] Визуальное разделение типов сообщений бота

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** Bot / Notifications

**Описание проблемы**
Все сообщения бота выглядят одинаково — подтверждения capture, напоминания, дайджест и системные сообщения визуально не различаются.

**Ожидаемое поведение**
Чёткое визуальное разграничение: подтверждения capture, точечные напоминания (срочное, с кнопками), дайджест (сводка), системные сообщения (нейтральный стиль).

**Технические детали**
- Унифицировать форматирование в `modules/notifications.py`
- При переходе на топики: добавить `message_thread_id` в вызовы `send_message`

**Критерии приемки**
- [ ] Каждый тип сообщений имеет устойчивый визуальный стиль
- [ ] Подтверждения capture, напоминания и дайджест визуально различимы

---

### [FEATURE-011] Напоминание о замене паспорта РФ в 45 лет

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** `modules/reference.py`, `models.py`

**Описание проблемы**
Паспорт РФ бессрочный — у него нет `end_date`, reminder не создаётся автоматически. При этом замена обязательна в 45 лет.

**Ожидаемое поведение**
Если в справочнике есть запись `person` с `birth_date` и добавляется паспорт РФ — вычислить дату замены (`birth_date` + 45 лет − 3 месяца) и создать entity с reminder.

**Технические детали**
- Логика вычисления: `date(birth_year + 45, birth_month, birth_day) - timedelta(days=90)`
- Связь через `owner_ref_id`: document → person

**Критерии приёмки**
- [ ] При добавлении паспорта РФ с имеющимся `person` с `birth_date` — entity создаётся с reminder
- [ ] Если `person` с `birth_date` не найден — entity создаётся без reminder (без ошибки)
- [ ] Тест покрывает вычисление даты замены

---

### [FEATURE-012] Кастомные сроки уведомлений по типу документа

**Статус:** 🆕 To Do
**Приоритет:** Medium
**Компонент:** `modules/reference.py`, `prompts/`

**Описание проблемы**
Все документы получают одинаковые напоминания — за 30, 14, 7 дней. Для загранпаспорта оптимально за 6 месяцев и 3 месяца, для ОСАГО — 30 дней достаточно.

**Ожидаемое поведение**
Напоминания создаются по правилам, зависящим от `doc_type` документа.

**Технические детали**
- Таблица правил: `dict[str, list[int]]` по `doc_subtype` в `modules/reference.py`
- Примеры: `загранпаспорт` → [180, 90], `осаго` → [30, 7], `права` → [30], `default` → [30, 14, 7]

**Критерии приёмки**
- [ ] `загранпаспорт` создаёт reminders за 180, 90 дней
- [ ] `осаго` создаёт reminders за 30, 7 дней
- [ ] Для неизвестного типа применяется default (30, 14, 7)
- [ ] Тест покрывает выбор правил

---

### [FEATURE-017] Мёрж двух билетов (туда + обратно) в одну поездку

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** `modules/reference.py`, `modules/lifecycle.py`

**Описание проблемы**
Пользователь загружает два PDF билета — туда и обратно. Создаются два отдельных entity типа trip с дублирующимися данными и двумя наборами напоминаний.

**Ожидаемое поведение**
Если два билета с одинаковым маршрутом (A→B и B→A) загружены подряд — смержить в одну entity: `start_date` из первого, `end_date` из второго.

**Acceptance Criteria**
- [ ] Два билета A→B и B→A → одна entity с start_date и end_date
- [ ] Предложить мёрж через inline кнопку, не автоматически
- [ ] `make check` проходит

---

### [FEATURE-018] Мёрж брони отеля + билетов в одну поездку

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** `modules/reference.py`, `modules/lifecycle.py`

**Описание проблемы**
Билет на самолёт и бронь отеля — одна поездка, но парсятся как два отдельных entity.

**Ожидаемое поведение**
При загрузке брони отеля — проверить есть ли trip entity с пересекающимися датами в том же направлении. Если да — предложить объединить.

**Acceptance Criteria**
- [ ] Бронь отеля с совпадающими датами + trip entity → кнопка «Объединить»
- [ ] При объединении notes отеля дописываются к entity
- [ ] `make check` проходит

---

## 🧊 Icebox

### [FEATURE-010] Мультипользовательность (v2)

**Статус:** 🆕 To Do
**Приоритет:** Low
**Компонент:** Auth / Data

**Описание проблемы**
owner_id в entities заложен, но логика одного пользователя. В v2 — семейный граф.

**Технические детали**
- Таблица users
- owner_id → реальный FK на users.id
- Семейные объекты: shared_with[]

---

## Прогресс

| Срез | Задача | Статус |
|------|--------|--------|
| 0 | TECH-001: Инициализация проекта | ✅ Done |
| 0 | TECH-002: Config + DB + Models | ✅ Done |
| 1 | FEATURE-001: Telegram Capture → БД | ✅ Done |
| 2 | FEATURE-002: Reminder Engine + Scheduler | ✅ Done |
| 3 | FEATURE-003: Suggestion Engine + Notifications | ✅ Done |
| 4 | FEATURE-004: Cloudflare R2 Storage | ✅ Done |
| 5 | FEATURE-005: Lifecycle Management | ✅ Done |
| 6 | FEATURE-006: Google Calendar | ✅ Done |
| 7 | FEATURE-007: API Stubs | ✅ Done |
| — | FEATURE-014: Карточки людей + привязка документов | ✅ Done |
| — | FEATURE-015: Несколько файлов на одну запись справочника | ✅ Done |
| — | FEATURE-016: Генерация чеклиста вещей для поездки | ✅ Done |
| — | ARCH-006: Конституция агента `soul.txt` | 🆕 To Do |
| — | UX-001: State machine для прикрепления файлов | 🆕 To Do |
| — | UX-004: Редактирование записи справочника | 🆕 To Do |
| — | UX-005: Dialog state для generate_text | 🆕 To Do |
| — | UX-006: Отправка файлов медиа-альбомом | 🆕 To Do |
| — | UX-007: Приём нескольких фото альбомом | 🆕 To Do |
| — | UX-008: FSM — матчинг поездки для чеклиста | 🆕 To Do |
| — | UX-009: FSM — уточнить тип поездки | 🆕 To Do |
| — | UX-010: Кнопка «Создать поездку» — callback | 🆕 To Do |
| — | FEATURE-012: Кастомные правила heartbeat | 🆕 To Do |
| — | FEATURE-013: Импорт событий из Google Calendar | 🆕 To Do |
| — | FEATURE-011: Напоминание о замене паспорта в 45 лет | 🆕 To Do |
| — | FEATURE-012: Кастомные сроки по типу документа | 🆕 To Do |
| — | FEATURE-017: Мёрж двух билетов в одну поездку | 🆕 To Do |
| — | FEATURE-018: Мёрж брони отеля + билетов | 🆕 To Do |
| — | ARCH-007: Skill-based маршрутизация | 🆕 To Do |
| — | ARCH-008: Уровни автономии (confirm-before-execute) | 🆕 To Do |
| — | OPS-003: Тихий heartbeat — логи `nothing to do` | 🆕 To Do |
| — | FEATURE-008: Email inbound (Mailgun) | 🧊 Icebox |
| — | FEATURE-009: Web UI | 🧊 Icebox |
| — | UX-002: Файлы на карточке объекта (Web UI) | 🧊 Icebox |
| — | UX-003: Визуальное разделение сообщений | 🧊 Icebox |
| — | FEATURE-010: Multi-user | 🧊 Icebox |
